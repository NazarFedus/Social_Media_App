import React, { FC } from "react";

import useStyles from './styles'

const Form: FC = () => {
     const classes = useStyles();
  return (
    <div>
      <h1>Form</h1>
    </div>
  );
};

export default Form;
