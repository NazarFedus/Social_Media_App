import React, { FC } from "react";

import useStyles from "./styles";

const Post: FC = () => {
  const classes = useStyles();

  return (
    <div>
      <h1>Post</h1>
    </div>
  );
};

export default Post;
